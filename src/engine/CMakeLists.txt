project(Engine)

file(GLOB_RECURSE ENGINE_SRC ${CMAKE_CURRENT_SOURCE_DIR} *.cpp)
file(GLOB_RECURSE ENGINE_INC ${CMAKE_CURRENT_SOURCE_DIR} *.h)

include_directories(${CMAKE_CURRENT_SOURCE_DIR})
add_library(engine ${ENGINE_SRC} ${ENGINE_INC})