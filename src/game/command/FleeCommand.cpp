#include "FleeCommand.h"

void FleeCommand::execute(dc::engine::CommandParameters &parameters) const {
    // determine the position we're going
    // if we can go that route
        // just go that route, man
    // notify that player that route is not the way
}
